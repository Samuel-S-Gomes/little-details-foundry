# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Node (caso use ferramentas de build futuramente)
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json

# Build / dist
dist/
*.zip

# Compêndios LevelDB (gerados pelo build, fontes ficam em packs/*/_source/)
packs/*/CURRENT
packs/*/LOCK
packs/*/LOG
packs/*/LOG.old
packs/*/MANIFEST*
packs/*/*.ldb
packs/*/*.log
packs/*/*.sst

# Token local (nunca enviar ao GitHub)
.token
